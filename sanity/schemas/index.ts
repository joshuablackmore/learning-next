import artwork from "./artwork-schema";
import blog from "./blog-schema";
import home from "./home-scheme";
import profilePic from "./profilePic-schema";

const schemas = [artwork, profilePic, home, blog];

export default schemas;
